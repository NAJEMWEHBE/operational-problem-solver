# Copy to .env and fill in. The engine reads ANTHROPIC_API_KEY from the environment.
# This is a PAID, metered key. A single run spawns several Claude calls (workers + judge).
ANTHROPIC_API_KEY=sk-ant-...

# Optional: enable SDK debug logging
# ANTHROPIC_LOG=info
